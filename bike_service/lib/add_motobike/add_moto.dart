import 'package:flutter/material.dart';
import 'package:flutter_complete_guide/add_motobike/components/body.dart';

class AddMotobike extends StatelessWidget {
  const AddMotobike({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Body());
  }
}
